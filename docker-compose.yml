services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: doula
      POSTGRES_PASSWORD: doula
      POSTGRES_DB: doula_billing
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
